#include<stdio.h>
int main()
{
  int n, i, c=0;
  if(n%2==0) n = n-1;
  
  for(i=n;i>1;i=i-2);
  {
    if(c>2)
      {
	break;
      }
    else
      {
	printf("%d, ",i);
	c++;
      }
  }
  return 0;
}
