#include<stdio.h>
int main()
{
  int x, y, z;
  if(z>=x && z<y)
    {
      printf("Z este in interiorul intervalului!");
    }
  else
    {
      printf("Z NU este in interiorul intervalului!");
    }
  return 0;
}
